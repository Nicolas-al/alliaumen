function banIp(){
var idComment = document.querySelectorAll('.comment');
var idStr = idComment.id;
console.log(idComment);

    // $.ajax({
    //     url: '/modo/' + id + '?action=deleteCom',
       
    // })
}
banIp();