function banIp(){
var idComment = document.querySelectorAll('.comment');
x = idComment.length;
var idStr = idComment[x].id;
console.log(idStr);

    // $.ajax({
    //     url: '/modo/' + id + '?action=deleteCom',
       
    // })
}
banIp();