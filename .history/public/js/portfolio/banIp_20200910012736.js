function banIp(){
var idComment = document.querySelector('#comments');
var idStr = idComment.id;
console.log(idStr);

    // $.ajax({
    //     url: '/modo/' + id + '?action=deleteCom',
       
    // })
}
banIp();